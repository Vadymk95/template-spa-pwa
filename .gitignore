# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
# Stryker mutation testing (the incremental report is a local/CI cache, not a source)
.stryker-tmp
reports
# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# Lighthouse-CI artefacts
/.lighthouseci/
*.local
.eslintcache
.prettiercache
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# MCP artifacts
.playwright-mcp/

# .cursor/ is tracked in this template (personal repo) — override the global
# ghost-mode ignore (~/.gitignore_global) so plain `git add` works for agents.
!.cursor/
!.cursor/**

# .claude/commands/ holds the repo's own agent commands and is tracked for the
# same reason. The directory itself has to be re-included first: the global
# ignore lists `.claude/`, and git will not descend into an ignored directory to
# see a negation inside it. Machine-local settings stay untracked.
!.claude/
!.claude/commands/
!.claude/commands/**
.claude/settings.local.json
.claude/settings.json
