---
description: Write tests for the change that hunt corner cases at integration seams, not the happy path
---

Canonical: `.claude/commands/test.md`. Read that file in full and follow it exactly.

This file is a pointer so the command behaves identically in Cursor and in Claude Code. Do not
duplicate the instructions here — edit the canonical file, never this shim.
