{
    "_description": "The gate discipline as data. scripts/trace-report.mjs reads this instead of hardcoding stage names; scripts/verify-push.mjs reads `phase`. Editing THIS file is how the discipline changes; the analyser names no stage. Docs: .cursor/brain/VERIFICATION.md (phases + triggers).",
    "phase": 0,
    "_phaseMeaning": "0 = scaffold (pre-deploy): the push gate runs audit + hooks + oxlint + format + types + lint + coverage and SKIPS build/pwa/web-vitals-chunks/size/e2e \u2014 there is no prod boundary to guard before the first deploy. 1 = deployed: the push gate runs the full verify:ci chain. Flip with a one-line commit at the FIRST DEPLOY; GATE_PHASE=full overrides per run (template maintainers push gate machinery with it).",
    "checks": {
        "verify:iter": {
            "class": "code",
            "description": "oxlint -> tsc incremental -> vitest --changed"
        },
        "verify": {
            "class": "code",
            "description": "hooks -> gate env -> oxlint -> format -> tsc -> lint -> coverage -> build -> pwa -> chunks -> size -> e2e:prod"
        },
        "verify:ci": {
            "class": "code",
            "description": "audit:gate -> verify"
        },
        "verify:full": {
            "class": "code",
            "description": "verify:ci -> smoke:dev"
        },
        "verify:push": {
            "class": "code",
            "description": "phase-aware push gate: phase 0 -> audit + verify:scaffold, phase 1 -> verify:ci"
        },
        "verify:scaffold": {
            "class": "code",
            "description": "phase-0 offline gate: hooks -> oxlint -> format -> tsc -> lint -> coverage (no build/pwa/size/e2e)"
        },
        "verify:measure": {
            "class": "measure",
            "description": "build + prod-mode look at the result; sanctioned at ANY moment, never a violation"
        },
        "lint": {
            "class": "code",
            "description": "eslint (cached)"
        },
        "test": {
            "class": "code",
            "description": "vitest run"
        },
        "test:e2e": {
            "class": "code",
            "description": "playwright test"
        },
        "build": {
            "class": "code",
            "description": "tsc -b && vite build"
        },
        "smoke:dev": {
            "class": "code",
            "description": "dev-server smoke on its own port"
        },
        "e2e:one": {
            "class": "code",
            "description": "one Playwright spec on a free port"
        },
        "test:one": {
            "class": "code",
            "description": "one vitest file"
        },
        "docs:check": {
            "class": "docs",
            "description": "mechanical doc drift: paths, scripts, sentinels, versions, command table, dead docs; the push budget vs traced pushes of the same phase (report only)"
        }
    },
    "docs": {
        "_description": "Data for scripts/docs-check.mjs. sentinels = verbatim fragments of the tier law that may live only in AGENTS.md; versions = doc names -> package names compared with package-lock.json (node -> .nvmrc).",
        "sentinels": [
            "never shortened by what the diff touched",
            "Legal at any time, in any lane, never a violation",
            "the pre-commit hook owns it",
            "Parallel lanes never run heavy stages",
            "A skipped stage is printed, never silent"
        ],
        "internalScripts": [
            ":raw$",
            "^verify:scaffold",
            "^lint:staged$",
            ":inner$",
            "^verify:web-vitals-chunks:full$"
        ],
        "versions": {
            "Vitest": "vitest",
            "Vite": "vite",
            "React": "react",
            "Playwright": "@playwright/test",
            "TypeScript": "typescript",
            "Tailwind": "tailwindcss",
            "Zustand": "zustand",
            "StrykerJS": "@stryker-mutator/core",
            "Stryker": "@stryker-mutator/core",
            "ESLint": "eslint",
            "Prettier": "prettier",
            "oxlint": "oxlint",
            "Node": "node"
        }
    },
    "suites": {
        "_description": "Ceilings on the browser suite. It is counted in INVARIANTS, not screens (AGENTS.md \u00a7 the gate): a route earns a browser test only when it brings an invariant the existing specs do not measure. A suite over its ceiling is a finding, and so is a ceiling more than twice the measurement \u2014 that high it flags nothing. Raise a number on a measurement plus a DECISIONS.md line, like a size budget.",
        "e2e": {
            "dir": "e2e",
            "match": "\\.spec\\.ts$",
            "count": "tests",
            "max": 18,
            "_measured": "13 on 2026-09-13"
        }
    },
    "moments": {
        "iterate": {
            "description": "The desk. Run per change.",
            "expected": ["verify:iter", "e2e:one", "test:one", "docs:check"],
            "forbidden": [
                "verify",
                "verify:ci",
                "verify:full",
                "verify:push",
                "verify:scaffold",
                "lint",
                "test",
                "test:e2e",
                "build",
                "smoke:dev"
            ],
            "budgetSeconds": 15,
            "runsRegardlessOfDiff": false,
            "requiresMainCheckout": false
        },
        "commit": {
            "description": "Husky pre-commit: staged autofix, TDD sibling gate, then repo-wide oxlint/format/tsc. Those run as direct commands, not traced npm scripts, so this moment is declared for completeness.",
            "expected": ["docs:check"],
            "forbidden": [],
            "budgetSeconds": 15,
            "runsRegardlessOfDiff": false,
            "requiresMainCheckout": true
        },
        "measure": {
            "description": "Answering a question only a rendered result can answer. Legal at any time, any lane; has a budget so a slow measurement is still a finding.",
            "expected": ["verify:measure"],
            "forbidden": [],
            "budgetSeconds": 180,
            "runsRegardlessOfDiff": true,
            "requiresMainCheckout": false
        },
        "push": {
            "description": "Husky pre-push -> verify:push (phase-aware). Runs unconditionally, never shortened by what the diff touched.",
            "expected": ["verify:push"],
            "forbidden": [],
            "runsRegardlessOfDiff": true,
            "requiresMainCheckout": true,
            "budgetWindow": 20,
            "budgetDriftRatio": 1.3,
            "budgetBaselineMinRuns": 8,
            "_budget": "NO SECONDS HERE, deliberately, and this matters more in a template than anywhere else. A committed number of seconds describes ONE machine and nothing in the file says which, so every fork inherits a ceiling it may be unable to meet and reads red from its third push - measured ratio between the workstation that set the old numbers and a two-core runner: 5.6x and 10.5x on the same two suites. The gate calibrates a baseline from its OWN first `budgetBaselineMinRuns` runs, per phase, keeps it in the gitignored .gate-budget.json beside the tracer log, and reports drift past `budgetDriftRatio`. The baseline ratchets DOWN by itself when the gate gets faster, the same shape a lint or type baseline uses; it rises only when someone deletes the file, which is the honest way to say this is the new normal. `budgetWindow` keeps one bad day from poisoning p90. A fresh clone starts with no baseline, no red reading, and calibrates to its own hardware."
        }
    }
}
